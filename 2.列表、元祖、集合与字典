列表、元祖、集合与字典
知识点：学习四种非常常用的数据结构(列表、元祖、集合与字典)的概念与操作

（一）列表：是一种有序的数据集合

 列表操作：
 1 append() 在列表的末尾添加一个新的元素； insert() 将数据插入列表的任意位置，如courses.insert(0，"python") 表示在courses列表第一个位置插入新元素python
 2 count(s) 会返回列表元素中s的数量
 3 remove() 在列表中移除任意指定值； del 关键字；删除列表指定位置的元素，需要用到列表要删除元素的索引；如del courses[-1]
 4 reverse() 反转列表
 5 extend()  将一个列表合并到另外一个列表的末尾位置，如 A.extend(B) B列表合并到A列表的末尾
 6 sort()  排序；前提列表元素可以比较，数字按照大小，字符床按照字母表的顺序
 7 pop() 返回最后一个元素，并且删除这个元素；也可以传入参数i，即pop(i)会将第i个元素弹出

 (二)元组：是一种特殊的列表，一旦创建就不能修改
  注意：创建只有一个元素的元组，元素后面要跟一个逗号

（三）集合：是一个无序不重复元素的数据集
     项目开发中，集合主要在数据元素的去重和测试是否存在；创建集合的方法是{}和 set函数，但是空的集合只能使用set函数创建，如course = set()
     集合操作：
     in not in：判断元素是否在集合中
     add() 增加元素；remove() 删除元素
     集合之间的运算：| & - ^ 
     |：存在set1中或set2中的元素，等于union操作（相当于是并集）
     &：返回即在set1又在set2的元素（相当于交集）
     —：set1 - set2返回在set1不在set2的元素
     ^： 返回只单独存在两个集合中的元素

(四) 字典：是无序的键值对集合。
    字典中的每一个元素都是一个key和一个value的组合，key值在字典中必须是唯一的,key的类型必须是不可变类型。
    操作：
    取值dict[key],dict.get(key)
    增加或修改：dict[key]=value
    删除元素：del dict[key],不存在抛异常
    dict.items() 获取所有的字典元素，返回的是 dict_items类型的对象
    keys()和values()分别只获取字典中所有的key或value的列表
    pop(key) 返回key对应的value，并将key:value键值对删除
    
